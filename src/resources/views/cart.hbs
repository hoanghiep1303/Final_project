{{#if messages}}
<div class="{{#if success}}bg-success{{else}}bg-danger{{/if}} shadow p-2 rounded">
    <span class="text-white h6">{{messages}}</span>
</div>
{{/if}}

{{!-- {{#if products}}
<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
        <div class="list-group">
            {{#each products}}
            <div class="list-group-item border">
                <strong>{{ this.item.name }}</strong>
                <span class="text-black badge price-cart">${{ this.price }}</span>
                <div class="btn-group">
                    <button type="button" class="btn btn-raised btn-info dropdown-toggle" data-bs-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        Action
                    </button>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="cart/reduce/{{ this.item._id }}">Reduce by 1</a>
                        <a class="dropdown-item" href="cart/remove/{{ this.item._id }}">Remove All</a>
                    </div>
                </div>
                <span class="total">{{ this.qty }}</span>
            </div>
            {{/each}}
        </div>
    </div>
</div>
<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
        <strong>Total: {{ totalPrice }}</strong>
    </div>
</div>
<hr>
<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
        <a href="/checkout" class="btn btn-raised  btn-success">Continue to checkout</a>
    </div>
</div>
{{else}}
<div class="empty">
    <h2>Oops, your cart is empty</h2>
    <img src="/icons/empty-cart.svg" alt="Empty cart">
</div>
{{/if}} --}}


<div class="container mt-5 mb-5">
    <div class="d-flex justify-content-center row">
        <div class="col-md-8">
            <h1 class="text-uppercase text-success text-center pages-title">Shopping cart</h1>
                {{#if products}}
                <div class="py-2 d-flex flex-row align-items-center pull-right">
                    <span class="me-1">Sort by:</span>
                    <span class="me-1 font-weight-bold"> Price</span>
                    <i class="fa fa-angle-down"></i>
                </div>
                <div class="list-group">
                    {{#each products}}
                    <div class="list-group-item p-0 my-2 rounded">
                        <div
                            class="d-flex flex-row justify-content-between align-items-center p-2 bg-white px-3 rounded">
                            <div class="me-1"><img class="rounded card-img-top" src="../../uploads/{{this.item.image}}" width="70">
                            </div>
                            <div class="d-flex flex-column align-items-center product-details">
                                <span class="font-weight-bold">{{ this.item.name }}</span>
                                <div class="d-flex flex-row product-desc">
                                </div>
                            </div>
                            <div class="d-flex flex-row align-items-center qty">
                                <a href="cart/reduce/{{ this.item._id }}" class="text-decoration-none"><i class="fa fa-minus text-danger"></i></a>
                                <h5 class="text-grey my-2 mx-3">{{ this.qty }}</h5>
                                <a href="cart/add-to-cart/{{ this.item._id }}" class="text-decoration-none"><i class="fa fa-plus text-success"></i></a>
                            </div>
                            <div>
                                <h5 class="text-grey my-2">${{ this.price }}</h5>
                            </div>
                            <a class="d-flex align-items-center text-decoration-none"
                                href="cart/remove/{{ this.item._id }}">
                                <i class="fa fa-trash text-danger"></i>
                                {{!-- <i class="fa fa-times text-danger" aria-hidden="true"></i> --}}
                            </a>
                        </div>
                    </div>
                    {{/each}}
                    <div class="row">
                        <div class="col-md-8 px-3 py-1">
                            <strong>Total: ${{ totalPrice }}</strong>
                        </div>
                    </div>
                </div>
                <div class="my-2 d-flex flex-row align-items-center p-2 bg-white rounded">
                    <input type="text" class="form-control border-0 gift-card" placeholder="discount code/gift card">
                    <a href="#" class="btn btn-outline-success btn-sm ml-2">
                        Apply
                    </a>
                </div>
                <a href="/checkout" class="w-100 mt-2 rounded-pill btn btn-success btn-block btn-lg ml-2 pay-button">
                    Continue to checkout
                </a>
                {{else}}
                <div class="card-body cart mt-4">
                    <div class="col-sm-12 text-center">
                        <img src="https://16stitches.com/assets/frontend/images/empty_cart.png" width="220" height="220" class="img-fluid mb-4 me-3">
                        <h3><strong>Your Cart is Empty</strong></h3>
                        <h4>Add something to checkout</h4>
                        <a href="/order" class="btn btn-success cart-btn-transform m-3" data-abc="true">Go to shopping</a>
                    </div>
                </div>
                {{/if}}
        </div>
    </div>
</div>